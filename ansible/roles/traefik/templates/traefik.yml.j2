api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":{{ traefik_http_port }}"

providers:
  file:
    directory: /etc/traefik/conf.d
    watch: true

log:
  level: {{ traefik_log_level }}
